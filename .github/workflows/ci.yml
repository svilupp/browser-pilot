name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-and-type:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Type check
        run: bun run typecheck

  test:
    name: Test
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        test-type: [unit, integration, cli]
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Install Chrome
        if: matrix.test-type != 'unit'
        id: setup-chrome
        uses: browser-actions/setup-chrome@v2
        with:
          chrome-version: 147

      - name: Verify Chrome installation
        if: matrix.test-type != 'unit'
        run: |
          echo "Chrome path: $CHROME_PATH"
          echo "Chrome version (action): $CHROME_VERSION_UNDER_TEST"
          "$CHROME_PATH" --version
          test "$("$CHROME_PATH" --version | grep -o '[0-9]\+' | head -1)" = "147"
        env:
          CHROME_PATH: ${{ steps.setup-chrome.outputs.chrome-path }}
          CHROME_VERSION_UNDER_TEST: ${{ steps.setup-chrome.outputs.chrome-version }}

      - name: Run ${{ matrix.test-type }} tests
        run: bun test:${{ matrix.test-type }}
        env:
          CI: true
          TEST_TIMEOUT: 60000
          CHROME_PATH: ${{ steps.setup-chrome.outputs.chrome-path }}
          BROWSER_PILOT_CHROME_PATH: ${{ steps.setup-chrome.outputs.chrome-path }}

      - name: Upload failure screenshots
        if: failure() && matrix.test-type != 'unit'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.test-type }}-failure-screenshots
          path: tests/screenshots/
          retention-days: 7
          if-no-files-found: ignore

  # Build check to ensure package builds correctly
  build:
    name: Build Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Build check (type check + verify exports)
        run: |
          bun run typecheck
          # Verify main entry point is valid
          bun -e "import('./index.ts')"
          echo "Build check passed!"
